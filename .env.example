# Backend service configuration
APP_NAME=translean
APP_BASE_URL=http://localhost:8000
BACKEND_PORT=8000
FRONTEND_PORT=5173
DB_PORT=5432
# For local dev use SQLite: sqlite+aiosqlite:///./translean.db
# For PostgreSQL use: postgresql+asyncpg://user:password@localhost:5432/translean
DATABASE_URL=sqlite+aiosqlite:///./translean.db
ALLOWED_ORIGINS=*
FORWARDED_ALLOW_IPS=*

# Whisper speech-to-text server
# - CPU only: Systran/faster-whisper-small or Systran/faster-whisper-medium
# - RTX 8GB: Systran/faster-whisper-medium or Systran/faster-whisper-large-v3-turbo
# - RTX 16GB+: Systran/faster-whisper-large-v3
# Note: 'large-v3' requires ~4.5-6GB VRAM. Use 'turbo' on 8GB cards to save space for the LLM.
WHISPER_BASE_URL=http://laptop-ip:7860
WHISPER_MODEL=Systran/faster-whisper-large-v3

# Ollama translation model server
# Recommended Models: 
#   - translategemma (Translation Specialist)
#   - qwen3 (Reasoning/Generalist)
# Hardware Tiering:
# - CPU only: translategemma:4b or qwen3:4b (Fallback: 3-6 tokens/sec)
# - RTX 8GB: translategemma:4b or qwen3:8b (Fits comfortably with Whisper Turbo)
# - RTX 16GB: translategemma:12b or qwen3:14b (Default; fits ~12-14GB combined)
# - RTX 24GB+: translategemma:27b or qwen3:32b (Highest fidelity; fits ~22GB total)
OLLAMA_BASE_URL=http://laptop-ip:11434
LLM_MODEL_TRANSLATION=translategemma:12b

# OIDC provider configuration
OIDC_ISSUER_URL=https://your-issuer.example.com
OIDC_CLIENT_ID=example-client
OIDC_CLIENT_SECRET=change-me
OIDC_REDIRECT_URI=http://localhost:5173/auth/callback
OIDC_SCOPE=openid email profile
# Example for Google OIDC Provider Configuration
# Get your Client ID/Secret at: https://console.cloud.google.com/apis/credentials
# OIDC_ISSUER_URL=https://accounts.google.com
# OIDC_CLIENT_ID=<your-google-client-id>.apps.googleusercontent.com
# OIDC_CLIENT_SECRET=<GOCSPX-your-google-client-secret>
# The Redirect URI must exactly match what you registered in the Google Console
# OIDC_REDIRECT_URI=http://<app-host:port>/auth/callback
# Standard Google scopes for identity
# OIDC_SCOPE=openid email profile

# Security: Set to false in local development, true in production
USE_SECURE_COOKIES=true
LOG_LEVEL=info
DEV_MODE=false